

ClassificationBasedTypeIndexer

generische basisklassen als suchziel neben basis,interf und attribut

TYPE-CACHE!!!!

AssembnlyLoadMethad asl hook (Indexer.Defaults indiziert Fusion-variante)

kein Bind mehr und kein parmeterloser konsruktur

SemanticDimension, ClassficationTag, RequireRuntimeFact

TYPEINDEXER weg von wireup hin zu getinstance + scopefx. GetInsctance(Of x as NEW) und ohne of MUSS(constructorargs as array, usePrivate!!!)

Umzug der Wgitelist in ein FilteringDimension (die dann als uterobjekt die Filteringstragy enthält)

Whiteliste aus ClassificationStraqtegy raus und eine approval stategy dazu (n)

"Suspend"-Funktion

Cache eines komplett eigenen DOM (Typ,Basistyp,Interfaces) in einer Datei +

TypeIndexer ITypeAnalysisStrategy mit/ohne Cahcing

Subscribe Methoden so erweitern, dass auch ein Typ-Abmelde-Delegate registriert werden können

ExtensionMethodIndexer bauen (auf basis des ExtensionmethodCache) der den AssemblyIndexer Verwendet

umbau des typesubscription, dass auch ein abmeldedelegat angegeben werden muss!!!! assemblyindexer funktion "Suspend_Assembly"
